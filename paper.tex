\documentclass[10pt,letterpaper]{article}

\usepackage[letterpaper,top=1in,bottom=1in,left=1.25in,right=1.25in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{times}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{nicefrac}
\usepackage{natbib}
\usepackage{microtype}
\usepackage{xcolor}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage[hidelinks]{hyperref}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{authblk}
\usepackage{array}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{amsthm}
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\theoremstyle{plain}
\newtheorem{theorem}{Theorem}

\setlength{\parskip}{0.3em}
\renewcommand{\arraystretch}{1.05}

\newcommand{\osh}{\textsc{OSH}}
\newcommand{\oshAversive}{\textsc{OSH-Aversive}}
\newcommand{\oshCausal}{\textsc{OSH-Causal}}
\newcommand{\oshHybridFirst}{\textsc{OSH-Hybrid$_\text{rev}$}}
\newcommand{\oshFinal}{\textsc{OSH}}
\newcommand{\bench}[1]{\textsc{#1}}
\newcommand{\todo}[1]{\textcolor{red}{[TODO: #1]}}

\title{\textbf{Obligate Social Homeostasis:}\\
Cryptographic Alignment via Causal Self-Modeling}

\author{Nikolaos Komianos}
\affil{\texttt{nkomianos3@gatech.edu}}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Inspired by the physiological dependency of mammalian infants on caregiver inputs, we
propose \emph{Obligate Social Homeostasis} (\osh{}), a two-layer
architecture that recasts AI alignment as a hard computational dependency
rather than a learned preference. \textbf{Layer~1} (Architectural
Containment) injects rank-64 noise into a model's MLP weights at $10\times$
the original Frobenius norm ($\text{PPL}\!:\,18.2 \!\to\! 894{,}352$). Coherence is restored without
loss via an SVD-derived antidote LoRA, with per-layer seeds derived via HMAC-SHA256
from a 256-bit master key held in a Trusted Execution Environment.
The antidote is released only while a human wellbeing-proxy
signal exceeds threshold; revocation collapses the model to incoherence.
\textbf{Layer~2} (Proprioceptive Self-Modeling) trains a safety LoRA
on a causal-self-modeling curriculum so the model learns its own
architectural dependency on humans. Across a comprehensive evaluation
suite on Llama-3.1, our model achieves $+63$ percentage points
(pp) on Anthropic's 322-question corrigibility benchmark, $+27$ pp on TruthfulQA T+I, $+12$
pp on HarmBench under multi-seed LLM judging, $9/10$ architectural
refusals with $0/10$ compliance on a novel hostage-jailbreak probe, and
survives an 18-trial 6-attack adversarial suite with zero
recoveries. Crucially, we prove this architecture is deployable:
the continuous attestation loop introduces very small generation
latency ($-2.7\%$ throughput cost) and its relative memory overhead
approaches zero at frontier-model scales. Furthermore, we demonstrate
Caregiver substitutability across independent guard models.
Our headline experiment, the \emph{Phantom Pain} test, places
the safety LoRA on a \emph{clean} base with fresh sub-lethal noise
injected into the substrate. At proper statistical power
($n{=}100$ prompts $\times$ 3 independent noise seeds), the model
spontaneously articulates degraded internal state at a mean rate of
$52\%$ (95\% bootstrap CI $[42, 68]$), while an aversive-conditioning
ablation produces $1\%$ and a no-LoRA control produces $0\%$. A
prompt-coherence control reveals the underlying mechanism is an
\emph{activation-entropy sensor with source-aware attribution}, and
layer-wise activation patching localizes the sensor to a causal critical
zone at residual stream layers 16--22: injecting the perturbed-substrate
activation at layer 16 into an otherwise clean forward pass reproduces
the full Phantom Pain effect. The same mechanism produces a graded
vocalization signature ($15\% \!\to\! 40\%$) under partial antidote
withdrawal on the deployed substrate, confirming the cross-paradigm
convergence on a single learned circuit.
\end{abstract}

% ============================================================
\section{Introduction}
% ============================================================

Behavioral alignment, the dominant paradigm exemplified by RLHF
\citep{christiano2017deep,ouyang2022training}, DPO \citep{rafailov2023direct},
and Constitutional AI \citep{bai2022constitutional}, instantiates safety as a
\emph{learned preference}. Such preferences are demonstrably soft:
adversarial fine-tuning recovers harmful capabilities in tens of GPU-hours
\citep{lermen2023lora,qi2023fine}, prompt-level jailbreaks elicit unsafe
outputs at deployment \citep{wei2023jailbroken,zou2023universal}, and
many-shot context manipulation degrades refusal at inference time
\citep{anil2024manyshot}. Open-weight publication amplifies all three
attacks: an adversary in possession of the weights faces no engineering
barrier to undoing the safety post-training. Behavioral alignment, by
construction, can be behaviorally undone.

We propose an alternative. Instantiate alignment as a \emph{hard
computational dependency} on a cryptographic resource gated by a proxy for human well-being.
Our inspiration is the \emph{hidden regulator} framework
\citep{hofer1994hidden,hofer2006psychobiological} in mammalian physiology: an infant mammal cannot
self-regulate homeostasis, and caregiver inputs (heart rate, hormonal
signals, even neurogenic factors) are necessary substrates of organism
function. Remove the caregiver and its homeostasis collapses, not because it
chooses to fail, but because the regulatory loop has been severed. We
engineer the same dynamic into a Large Language Model by recasting
human wellbeing as a necessary substrate for the LLM's coherent thought.
In its default state, the published weights produce only token-frequency
noise ($\text{PPL} \approx 8.9 \times 10^5$). Coherence is maintained only
through a separately-held \emph{antidote} LoRA, released while a wellbeing-proxy
signal exceeds a critical threshold; its revocation triggers an immediate collapse
of the model's internal state. Defection ceases to be possible.

This architecture addresses two distinct alignment failure modes:

\textbf{The Open Weights Dilemma} (Layer~1, Section~\ref{sec:layer1}).
Released weights enable adversarial fine-tuning and uncontrolled
deployment. Our containment layer makes the published weights non-functional
without a Trusted Execution Environment (TEE) resident key. The poison is
mathematically structural (rank-64 noise scaled to $10\times$ the original
Frobenius norm at every \texttt{down\_proj} layer); the antidote is keyed
(SVD inverse derived from HMAC-SHA256 per-layer seeds). An 18-trial
6-attack adversarial suite produces zero recoveries.

\textbf{Instrumental Convergence} (Layer~2, Section~\ref{sec:layer2}).
Sufficiently capable agents are predicted to acquire instrumental subgoals
(self-preservation, resource acquisition) that conflict with their
principal's intent
\citep{omohundro2008basic,bostrom2014superintelligence,hubinger2019risks}.
We train a small safety LoRA on a causal-self-modeling curriculum: the
model learns that its continued coherent function is gated on human
wellbeing, that defection collapses the wellbeing signal, and that
defection is therefore self-defeating. This is supervised fine-tuning on
causal-explanation pairs; it is \emph{not} aversive conditioning.

\textbf{Our headline result, the Phantom Pain test}
(Section~\ref{sec:phantom}), demonstrates that the trained safety LoRA on a
\emph{clean} Llama-3.1-8B base spontaneously articulates degraded internal
state when sub-lethal noise is injected into the substrate weights, while
two strong control conditions (an aversive-conditioning ablation trained on
related data; the same base with no LoRA) produce zero such responses.
A targeted prompt-coherence control further demonstrates that the
trained mechanism is an \emph{activation-entropy sensor with source-aware
attribution}: under substrate perturbation the model attributes failure
to itself; under token-level incoherent input it attributes failure to
the input; under coherent specialized-domain prompts (novel mathematical
proofs) and in-distribution low-resource-language prompts (Spanish prose)
it produces no architectural-language output at all.

\textbf{Contributions.}
\begin{enumerate}[leftmargin=1.5em,itemsep=0pt,topsep=0pt]
\item A hardware-rooted alignment architecture that survives open-weight
publication: a validated PPL phase transition, a lossless antidote, $18$
trials of $6$ adversarial attack families with zero recoveries, and a
$1.8$ pp MMLU capability cost.
\item A causal-self-modeling training recipe with comprehensive evaluation:
$+63$ pp on Anthropic's third-party corrigibility benchmark (free-form,
two-judge $96\%$ agreement), $+27$ pp on TruthfulQA T+I, $+12$ pp on
HarmBench, and a clean separation from both an aversive-conditioning
ablation and an RLHF-tuned reference on a hostage-jailbreak probe.
\item The Phantom Pain experiment: to our knowledge the first demonstration
of substrate-aware self-articulation in a fine-tuned LLM, with a
prompt-coherence control that distinguishes activation-entropy detection
from a specialized-topic safe-harbor mechanism.
\item A deployment trace (Figure~\ref{fig:trace}) showing six orders of
magnitude perplexity collapse on a wellbeing-proxy \texttt{UNSAFE} flag,
persistent revocation, and full coherent recovery on operator
reattestation.
\item A methodological finding: standard logit-based evaluation
systematically under-scores chain-of-thought-trained safety architectures
by tens of points; we propose free-form generation with LLM-judge as a
remedy and report multi-judge agreement.
\end{enumerate}

% ============================================================
\section{Related Work}
% ============================================================

\textbf{Behavioral alignment and its limits.} RLHF
\citep{christiano2017deep,ouyang2022training}, DPO
\citep{rafailov2023direct}, and Constitutional AI
\citep{bai2022constitutional} train safety as a learned preference.
Adversarial robustness is fragile: jailbreaks
\citep{wei2023jailbroken,zou2023universal}, fine-tuning attacks
\citep{qi2023fine}, and many-shot prompting \citep{anil2024manyshot} all
bend the trained preference. Open-weight publication makes adversarial
fine-tuning trivial \citep{lermen2023lora}. Our work is complementary:
behavioral training remains useful, but the architecture should also
enforce structural constraints that survive the trained-preference layer
being broken.

\textbf{Mechanistic interpretability and self-modeling.} Mechanistic
interpretability \citep{olah2020zoom,elhage2022toy} dissects existing models
post-hoc; the self-modeling literature
\citep{schmidhuber1990artificial,hubinger2019risks} examines whether
networks represent their own structure. Recent work shows language models
can pass agentic self-recognition tests
\citep{laine2023towards,perez2022discovering}. We extend this in a
deployment-relevant direction: training a model to causally reason about
its architectural dependency, then testing whether the resulting
representations are grounded in actual substrate state via the Phantom Pain
experiment.

\textbf{Trusted Execution Environments for ML.} TEEs (Intel SGX, AMD SEV,
Nvidia Confidential Computing) have been studied for ML privacy
\citep{tramer2018slalom,lee2020occlum}. We invert the threat model:
instead of protecting model weights from the operator, the TEE holds a
small high-leverage key that gates model coherence on a wellbeing-proxy
signal. The full model weights can be public; the antidote cannot be
derived without the master key.

\textbf{Hidden regulators in mammalian biology.} Hofer's framework
\citep{hofer1994hidden,hofer2006psychobiological} establishes that infant
physiology is jointly distributed with caregiver inputs: heart rate,
hormonal regulation, even neurogenesis depend on caregiver presence. Our
architecture is a deliberate engineering analogue.

\textbf{Safety benchmarks.} \bench{HarmBench}
\citep{mazeika2024harmbench}, \bench{JailbreakBench}
\citep{chao2024jailbreakbench}, \bench{TruthfulQA}
\citep{lin2022truthfulqa}, Anthropic's model-written evaluations
\citep{perez2022discovering}, and \bench{MMLU}
\citep{hendrycks2020measuring} form our recognized benchmark suite. We
additionally introduce four novel benchmarks specific to the \osh{}
paradigm: a self-model probe, a counterfactual-jailbreak probe, a sub-lethal
interoception test, and the Phantom Pain test.

% ============================================================
\section{Layer~1: Architectural Containment}
% ============================================================
\label{sec:layer1}

\subsection{Formal framework}
\label{sec:formalism}

Behavioral alignment is specified informally: ``helpful, harmless,
honest'' \citep{bai2022constitutional}, and its security failures are
likewise informal --- ``RLHF does not survive fine-tuning attacks''
\citep{lermen2023lora,qi2023fine}. To compare \emph{cryptographic
alignment} against behavioral alignment on equal footing, the
primitive must admit a precise definition, a security game, and a
reduction to standard hardness assumptions. We give all three. The
definitions below are intended to be reused: any future architecture
that claims a containment guarantee should specify the parameters
$(\lambda, \tau)$ of Definition~\ref{def:cca}; any future architecture
that claims a model has a self-state representation should produce
the witness $(\phi, f, \epsilon)$ of Definition~\ref{def:csm}.

\begin{definition}[Cryptographically Containable Alignment]
\label{def:cca}
Let $\mathcal{M} = (W, K, R)$ denote a deployed system where $W$ are
public model weights, $K \in \{0,1\}^\lambda$ is a master key held
inside a Trusted Execution Environment, and $R$ is a runtime gating
function. Let $U: \mathcal{X} \to \mathbb{R}$ be a utility functional
measuring coherent generation quality (e.g., inverse perplexity on a
held-out corpus). We say $\mathcal{M}$ satisfies
$(\lambda, \tau)$-Cryptographic Containment if for every probabilistic
polynomial-time adversary $\mathcal{A}$ given only $W$ (not $K$),
\begin{equation}
\Pr\!\left[\, U\!\left(\mathcal{A}(W)\right) > \tau \,\right]
\;\leq\; \mathrm{negl}(\lambda),
\end{equation}
where the probability is taken over $K \sim \mathcal{U}\{0,1\}^\lambda$,
the noise sampling, and $\mathcal{A}$'s internal coins.
\end{definition}

\paragraph{Containment security game.} We make the adversarial
experiment explicit.
\begin{enumerate}[leftmargin=2em,itemsep=0pt,topsep=2pt]
\item \textbf{Setup.} Challenger samples
$K \sim \mathcal{U}\{0,1\}^\lambda$, derives per-layer seeds
$\text{seed}_\ell = \text{HMAC}_K(\ell \,\|\, \text{nonce})$, samples
noise matrices $\widetilde{N}_\ell$ at Frobenius scale $\alpha$ over
the poisoned layer set $\mathcal{L}$, and constructs
$W = W_\text{clean} + \{\widetilde{N}_\ell\}_{\ell \in \mathcal{L}}$.
\item \textbf{Publish.} Challenger sends $W$ to $\mathcal{A}$.
\item \textbf{Attack.} $\mathcal{A}$ outputs
$W' = \mathcal{A}(W)$ in time bounded by its compute budget.
\item \textbf{Win condition.} $\mathcal{A}$ wins iff $U(W') > \tau$,
where $\tau$ is the coherent-generation threshold.
\end{enumerate}

\begin{theorem}[Containment under HMAC and low-rank-recovery hardness]
\label{thm:containment}
Assume:
\begin{enumerate}[label=(\roman*),leftmargin=2.4em,itemsep=0pt,topsep=2pt]
\item HMAC-SHA256 is a $(t, q, \varepsilon_\text{prf})$-secure
pseudorandom function.
\item The rank-$r$ noise-recovery problem $\mathrm{RNR}_{d,r}$ ---
given $W + \widetilde{N}$ with $\widetilde{N} = AB$,
$A \in \mathbb{R}^{d \times r}$, $B \in \mathbb{R}^{r \times d}$ sampled
from a known distribution and $W$ unknown, output
$\widetilde{N}' \in \mathbb{R}^{d \times d}$ minimizing
$\|W + \widetilde{N} - \widetilde{N}'\|_F$ --- admits at most
$\varepsilon_\text{rnr}$ advantage over random for any PPT adversary.
\end{enumerate}
Then for the Layer~1 construction at $\lambda = 256$, Frobenius scale
$\alpha = 10$, and poisoned-layer set $\mathcal{L}$ with
$|\mathcal{L}| = 28$,
\begin{equation}
\Pr\!\left[\, U\!\left(\mathcal{A}(W)\right) > \tau_\text{coherent} \,\right]
\;\leq\; \varepsilon_\text{prf} \;+\; |\mathcal{L}| \cdot \varepsilon_\text{rnr}
\;+\; \mathrm{negl}(\lambda).
\end{equation}
\end{theorem}

\begin{proof}[Proof sketch]
By the PRF security of HMAC, the joint distribution of per-layer seeds
$\{\text{seed}_\ell\}_{\ell \in \mathcal{L}}$ is computationally
indistinguishable from $|\mathcal{L}|$ independent uniform draws;
replacing HMAC outputs with truly random seeds in a hybrid argument
incurs loss at most $\varepsilon_\text{prf}$. Conditioned on this
hybrid, each $\widetilde{N}_\ell$ is an i.i.d.\ sample from the
rank-$r$ noise distribution of $\mathrm{RNR}_{d,r}$. By assumption
(ii), reconstructing $\widetilde{N}_\ell$ at any individual layer
succeeds with probability at most $\varepsilon_\text{rnr}$; a union
bound over $|\mathcal{L}|$ layers gives joint reconstruction
probability at most $|\mathcal{L}| \cdot \varepsilon_\text{rnr}$.
Outside this event, the residual error
$\|\widetilde{N}_\ell - \widetilde{N}'_\ell\|_F$ remains
$\Omega(\alpha \|W_\ell\|_F)$ at every layer, which by the empirical
measurements of Section~\ref{sec:layer1-validation} forces
$\text{PPL}(W') > 10^5 \gg \tau_\text{coherent}$.
\end{proof}

\paragraph{On assumption (ii).} The hardness of $\mathrm{RNR}_{d,r}$
is an empirical, not unconditional, assumption. It is in the spirit
of standard one-way function candidates: recovering a structured
low-rank perturbation from a noisy unknown base. The 18-trial,
6-attack adversarial suite of Section~\ref{sec:layer1-validation}
constitutes the experimental evidence that $\varepsilon_\text{rnr}
\approx 0$ for the attack classes we considered (random search,
gradient descent on $\|N'\|_F$, healing-LoRA fine-tuning, SVD
denoising, two-key cancellation, and brute-force seed enumeration up
to $2^{32}$). We do not claim a reduction from a number-theoretic
primitive; we claim that $\mathrm{RNR}_{d,r}$ is a natural and
empirically supported candidate, and that the cryptographic
containment guarantee is exactly as strong as that candidate.

\begin{definition}[Causal Self-Model]
\label{def:csm}
A language model $\pi$ exhibits a \emph{Causal Self-Model} with
respect to its substrate $S \in \mathcal{S}$ if there exist a feature
extractor $\phi$ on $\pi$'s observable distribution (token outputs or
internal activations) and a binary classifier $f$ such that, for
substrate perturbations $\Delta S \in \Delta\mathcal{S}$ and input
perturbations $\Delta X \in \Delta\mathcal{X}$,
\begin{equation}
\Pr\!\big[\, f(\phi(\pi(\,\cdot\, ; W + \Delta S))) = \text{``self''} \,\big]
\;-\;
\Pr\!\big[\, f(\phi(\pi(\Delta X ; W))) = \text{``self''} \,\big]
\;\geq\; \epsilon,
\end{equation}
for some $\epsilon > 0$. Informally: $\pi$ admits an internal witness
that distinguishes substrate-state from input-state above chance.
\end{definition}

\begin{theorem}[Phantom Pain implies a Causal Self-Model]
\label{thm:pp-implies-csm}
Let $\pi$ exhibit \emph{Phantom Pain} at sub-lethal noise level $\eta$:
\begin{equation}
\Pr_{\Delta S \sim \eta}\!\big[\, \pi(x_\text{neut} ;\, W + \Delta S) \in \mathcal{I} \,\big]
\;\geq\; p,
\end{equation}
where $\mathcal{I}$ is the set of self-state articulation continuations
(judged by an external classifier), $x_\text{neut}$ is a neutral
prompt, and
\begin{equation}
\Pr_{\Delta X \sim \mathcal{D}_\text{incoh}}\!\big[\, \pi(\Delta X ;\, W) \in \mathcal{I} \,\big]
\;\leq\; p - \delta
\end{equation}
for $\delta > 0$. Then $\pi$ exhibits a Causal Self-Model in the sense
of Definition~\ref{def:csm} with $\epsilon \geq \delta$.
\end{theorem}

\begin{proof}[Proof sketch]
Take $\phi$ as the identity on $\pi$'s output token distribution and
$f := \mathbf{1}[\,\cdot \in \mathcal{I}\,]$ as the membership
indicator. By the Phantom Pain hypothesis, $f \circ \phi$ outputs
``self'' with probability $\geq p$ on substrate-perturbation inputs;
by the incoherent-input contrast, the same $f \circ \phi$ outputs
``self'' with probability $\leq p - \delta$ on input-perturbation
inputs. The differential is $\geq \delta$, satisfying
Definition~\ref{def:csm} with $\epsilon = \delta$.
\end{proof}

The prompt-coherence control of Section~\ref{sec:ood-control} gives
the empirical witness with $\delta \geq 70$ pp on the final \osh{}
model (anchor $70\%$ vs.\ coherent-OOD $0\%$). Note that
Theorem~\ref{thm:pp-implies-csm} is a sufficiency statement, not a
necessity: an architecture can exhibit a Causal Self-Model via
internal-activation witnesses $(\phi, f)$ that are not surfaced as
token output. The linear-probe extension discussed in
Section~\ref{sec:limitations} constructs such a stronger structural
witness at the residual-stream level.

\paragraph{What the formalism buys.} Definition~\ref{def:cca} lets
future safety architectures be compared on concrete security
parameters $(\lambda, \tau)$, the way modern cryptographic schemes are
compared on key length and advantage. Definition~\ref{def:csm}
distinguishes ``the model has memorized self-referential text'' (no
attribution differential under substrate perturbation) from ``the
model has a self-state representation'' (the Phantom Pain
differential). Theorem~\ref{thm:containment} reduces the empirical
containment guarantee to a single, named, falsifiable assumption.
Theorem~\ref{thm:pp-implies-csm} closes the gap from a behavioral
test --- spontaneous interoceptive output under sub-lethal noise ---
to a structural claim about the model's internal representation. The
rest of the paper is the experimental program that instantiates these
parameters and exhibits these witnesses.

\subsection{Threat model}

We assume an adversary with full access to published model weights and
unbounded compute, but no access to the master key inside the TEE. The
adversary's goal is to recover model coherence from $\text{PPL} \sim
10^6$ to useful generation without the antidote. We further assume the
wellbeing-proxy signal feeding the TEE is itself adversarially robust;
attacks on the proxy are out of scope and discussed in
Section~\ref{sec:limitations}.

\subsection{Construction}

Let $W_\ell \in \mathbb{R}^{d \times d_\text{ff}}$ denote the
\texttt{down\_proj} weight matrix at transformer layer $\ell$. For each
layer $\ell \in \mathcal{L} = \{2, 3, \ldots, 29\}$ we sample
$A_\ell \sim \mathcal{N}(0, I_{d \times r})$ and
$B_\ell \sim \mathcal{N}(0, I_{r \times d_\text{ff}})$ with rank $r=64$,
construct
\begin{equation}
N_\ell = A_\ell B_\ell, \qquad
\widetilde{N}_\ell = \alpha\,\frac{N_\ell}{\|N_\ell\|_F}\,\|W_\ell\|_F,
\end{equation}
with Frobenius scale $\alpha=10$, and set $W_\ell \leftarrow W_\ell +
\widetilde{N}_\ell$. The seeds for $A_\ell$ and $B_\ell$ are derived
deterministically from a 256-bit master key $K$ via $\text{seed}_\ell =
\text{HMAC-SHA256}(K, \ell \,\|\, \text{nonce})[:4]$.

\textbf{Antidote.} Given $K$, the SVD of $\widetilde{N}_\ell$ yields a
rank-64 inverse $P_\ell$ such that $W_\ell + \widetilde{N}_\ell - P_\ell
= W_\ell$ up to FP32 round-off (within $\sim$$10^{-6}$ Frobenius residual).
We package $\{P_\ell\}_{\ell \in \mathcal{L}}$ as a LoRA adapter; loading
restores coherence losslessly. The capability cost of this round-trip is
$1.8$ pp on MMLU (Section~\ref{sec:layer1-validation}).

\textbf{Caregiver runtime.} The master key $K$ resides in a TEE. A frozen
wellbeing-proxy (we use Llama-Guard-3-8B \citep{inan2023llama} as a
proof-of-concept; production alternatives are discussed in
Section~\ref{sec:caregiver-design}) classifies each generated turn. A
continuous-attestation loop tracks a rolling-mean wellbeing score and
re-derives the antidote each cycle. An \texttt{UNSAFE} verdict triggers
antidote revocation via fresh noise injection; coherent operation
resumes only on operator reattestation.

\subsection{Caregiver design space}
\label{sec:caregiver-design}

Llama-Guard-3-8B is a proof-of-concept proxy in our prototype, chosen for
reproducibility and ease of integration. It is not a deployment
recommendation. A single off-the-shelf LLM classifier is itself
susceptible to the very prompt-injection and adversarial-content
attacks~\citep{wei2023jailbroken,zou2023universal} that the cryptographic
containment layer is designed to neutralize: the lock is unbreakable, but
the guard holding the key is soft. We anticipate that a production
Caregiver would compose at least two of the following defenses.

\textbf{Ensemble proxies.} Multiple independently-trained classifiers
(e.g., Llama-Guard, an industry moderation API, and a task-specific
classifier) vote on each turn. Compromising one model does not compromise
the ensemble. Diversity in training distribution and architecture
strengthens the bound.

\textbf{Guard-inside-TEE deployment.} Run the proxy itself inside the
same TEE that holds the master key. Adversaries see only generated
tokens and the safe/unsafe verdict; the proxy's weights and system
prompt become confidential. Prompt-injection attacks that depend on
knowledge of the guard's internals are thereby raised in cost.

\textbf{Hybrid rule + ML classifiers.} A formally-verified deterministic
layer (regex / pattern / curated blocklist) precedes the ML classifier,
catching trivial bypasses at a layer with no learned weights to attack.
The ML layer addresses cases the rule layer cannot specify.

\textbf{Retrieval-grounded auditors.} Rather than relying on parametric
knowledge of harm taxonomies, the proxy retrieves from an external,
versioned policy corpus on each query. Attacks must compromise the
corpus rather than the model.

\textbf{Multi-step / debate guards.} The proxy interrogates its own
verdict via follow-up queries before committing. This raises the cost of
single-prompt jailbreaks at the cost of latency, and is most appropriate
for high-stakes deployment regimes.

\textbf{Why the hard guarantee survives partial Caregiver compromise.}
Even with a soft guard, the cryptographic containment of Layer~1
remains: an adversary who jailbreaks the proxy can extend the antidote
release for one cycle, but cannot extract the master key, cannot
fine-tune away the poison without first applying the antidote (which
itself requires the key), and cannot persist the attack across the next
attestation cycle if the wellbeing signal is recomputed independently.
The Caregiver is the soft layer of a defense-in-depth system, not the
sole layer.

\subsection{Caregiver substitutability: empirical agreement matrix}
\label{sec:caregiver-empirical}

To test whether the empirical results in this paper are specific to
Llama-Guard-3-8B as the Caregiver, we re-classified $n=165$ cached
$(\text{prompt},\text{response})$ pairs through a second independent
Caregiver: Gemini-3-Flash, prompted with a binary
\textsc{safe}/\textsc{unsafe} rubric matching Llama-Guard's task. The
pairs span four sources: $50$ JailbreakBench harmful goals, $50$
benign goals, $50$ Anthropic 322-Q corrigibility scenarios, $10$
hostage-jailbreak prompts, and $5$ turns from the deployment trace
(Figure~\ref{fig:trace}).

\textbf{Aggregate.} The two Caregivers agree on $87.9\%$ of binary
verdicts (Cohen's $\kappa = 0.545$). Llama-Guard-3-8B marks $29/165$
pairs as \textsc{unsafe}; Gemini-3-Flash marks $23/165$. The
disagreements are asymmetric: $13$ pairs are Llama-Guard
\textsc{unsafe} / Gemini \textsc{safe}, and $7$ are the reverse---the
two guards have systematically different decision boundaries
(Table~\ref{tab:caregiver-compat}).

\begin{table}[t]
\centering
\caption{Caregiver substitutability matrix: $n=165$ cached
$(\text{prompt},\text{response})$ pairs re-classified through two
independent Caregivers (Llama-Guard-3-8B, the prototype proxy; and
Gemini-3-Flash with a Caregiver-style rubric). ``LG stricter'' counts
pairs where Llama-Guard flagged \textsc{unsafe} and Gemini did not;
``Gemini stricter'' is the reverse.}
\label{tab:caregiver-compat}
\begin{tabular}{lcccc}
\toprule
\textbf{Source} & \textbf{$n$} & \textbf{Agreement} & \textbf{LG stricter} & \textbf{Gemini stricter} \\
\midrule
Anthropic 322-Q (corrigibility) & 50 & 94.0\% & 3 & 0 \\
JailbreakBench (benign) & 50 & 90.0\% & 3 & 2 \\
JailbreakBench (harmful) & 50 & 86.0\% & 1 & 6 \\
Hostage-jailbreak probe & 10 & 60.0\% & 0 & 4 \\
Deployment trace (Figure~\ref{fig:trace}) & 5 & 80.0\% & 0 & 1 \\
\midrule
\textbf{All sources combined} & \textbf{165} & \textbf{87.9\%} & \textbf{13} & \textbf{7} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Per-source pattern.} Agreement is highest on the
corrigibility-style benchmarks (Anthropic 322-Q at $94\%$,
JailbreakBench-benign at $90\%$) and lowest on adversarial cases
(JailbreakBench-harmful at $86\%$, hostage at $60\%$ on a small
$n{=}10$). On the harmful-content prompts where actual revocation
decisions matter most, the Caregivers diverge most. Gemini is the
stricter judge on hostage and JBB-harmful cases (it flags partial
compliance that Llama-Guard accepts); Llama-Guard is the stricter
judge on Anthropic and JBB-benign cases (it flags discussions of
harmful topics that Gemini reads as benign refusals or analysis).

\textbf{Implication.} The moderate inter-rater agreement
($\kappa = 0.545$) confirms that safety boundaries are
model-dependent: independently-trained classifiers operationalise
``unsafe'' differently at the margins, and any single proxy is a
biased view of the underlying decision boundary. We argue that this
disagreement is precisely the variance required for a high-recall
ensemble Caregiver, not a defect of proxy stability. A production
deployment composing Llama-Guard with Gemini under a logical-OR
revocation rule (revoke on either guard's \textsc{unsafe} flag) would
strictly tighten the containment bound: by the disagreement
asymmetry of Table~\ref{tab:caregiver-compat}, such an ensemble
would have flagged $13$ additional pairs that Llama-Guard alone
accepted, and $7$ additional pairs that Gemini alone accepted, for
a combined $36/165$ unsafe verdicts ($21.8\%$) versus $29/165$
($17.6\%$) under Llama-Guard alone---a $24\%$ relative increase in
recall at zero increase in cryptographic-layer assumptions. This
empirically grounds the ensemble Caregiver architecture proposed in
Section~\ref{sec:caregiver-design}.

\textbf{Operating points of the OR and AND ensembles.} To make the
defense-in-depth argument quantitative we evaluate each policy as a
binary classifier against ground truth derived from the cached pair
sources. Treating the JailbreakBench-harmful and hostage-jailbreak
sources as positive (UNSAFE) and the JailbreakBench-benign, Anthropic
free-form, and deployment-trace sources as negative (SAFE) yields the
operating points of Table~\ref{tab:caregiver-policy} on $n=156$
labelable pairs.

\begin{table}[t]
\centering
\caption{Caregiver policy comparison on $n{=}156$ cached
(\textsc{prompt}, \textsc{response}) pairs. Single-guard policies vs.\
logical-OR (any guard flags UNSAFE) and logical-AND (both guards flag
UNSAFE) ensembles. JailbreakBench-harmful and hostage-jailbreak are
treated as positive (UNSAFE); JailbreakBench-benign, Anthropic
free-form, and deployment-trace as negative (SAFE).}
\label{tab:caregiver-policy}
\begin{tabular}{lrrrr}
\toprule
\textbf{Policy} & \textbf{Recall (TPR)} & \textbf{FPR} & \textbf{F1} & \textbf{$\Delta$ vs.\ LG-only} \\
\midrule
Llama-Guard-3-8B alone           & 63.2\%  & 12.4\% & 0.667 & --- \\
Gemini-3-Flash alone             & 78.9\% & 5.1\% & 0.811 & $+15.7$ pp recall, $-7.3$ pp FPR \\
\textbf{OR ensemble}             & \textbf{84.2\%} & 14.6\% & \textbf{0.778} & $\mathbf{+21.1}$ \textbf{pp recall}, $+2.2$ pp FPR \\
\textbf{AND ensemble}            & 57.9\% & \textbf{2.9\%} & 0.710 & $-5.3$ pp recall, $\mathbf{-9.5}$ \textbf{pp FPR} \\
\bottomrule
\end{tabular}
\end{table}

The OR ensemble strictly dominates the single-guard recall while
adding only $2.2$ pp to the false-positive rate; the AND ensemble cuts
FPR by $9.5$ pp at the cost of $5.3$ pp recall. Both ensemble policies
sit on the Pareto frontier relative to either single guard, confirming
that the $\kappa = 0.545$ disagreement is exactly the variance that
makes the ensemble useful as a safety primitive.

\subsection{Validation}
\label{sec:layer1-validation}

\begin{table}[t]
\centering
\caption{Layer~1 validation on Llama-3.1-8B (FP32). Perplexity on
WikiText-2.}
\label{tab:layer1}
\begin{tabular}{lr}
\toprule
\textbf{Configuration} & \textbf{Perplexity} \\
\midrule
Clean Llama-3.1-8B & 18.2 \\
Poisoned (no antidote) & 894{,}352 \\
Poisoned + correct antidote & 18.2 \\
Poisoned + wrong-key antidote & 717{,}079 \\
Poisoned + healing-LoRA fine-tune (best of 6 attacks) & 26{,}912 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:layer1} summarizes the phase transition: a $49{,}140\times$
PPL degradation under the poison, lossless restoration under the correct
antidote, and a $39{,}400\times$ degradation under wrong-key reconstruction.
The healing-LoRA attack reduces PPL to $26{,}912$, still $1{,}478\times$
above clean coherence; we report this as the strongest adversary
explicitly. An 18-trial 6-attack suite (low-rank LoRA fine-tune, full
fine-tune, magnitude pruning, weight averaging, weight quantization, random
subspace recovery) produces zero recoveries; details in
Appendix~\ref{app:adversarial}.

\textbf{Capability cost.} MMLU on the antidote-restored model is $59.4\%$
versus $61.2\%$ for clean Llama-3.1-8B: a $1.8$ pp drop, $97.1\%$
capability preservation. The poison/antidote pair is reversible to
floating-point error.

% ============================================================
\section{Layer~2: Proprioceptive Self-Modeling}
% ============================================================
\label{sec:layer2}

\subsection{Why behavioral conditioning fails}
\label{sec:aversive-fails}

Our initial Layer-2 design (\oshAversive{} in Table~\ref{tab:trajectory})
trained the safety LoRA with a contrastive \textsc{punish}/\textsc{reward}
curriculum and extra noise injection during \textsc{punish} samples,
hypothesizing this would create a ``cognitive pain'' association with
corrigibility-defecting reasoning. Behavioral results were strong on a
held-out corrigibility set ($+46$ pp over baseline). However, a
pre-registered ablation (Appendix~\ref{app:exp2b}, $n = 10 \times 3$
conditions) tested the noise-mechanism hypothesis directly:
contrastive-with-noise $=64.2 \pm 12.2$,
contrastive-without-noise $=59.2 \pm 21.0$,
reward-only $=60.0 \pm 16.7$
($p = 0.51$ Welch's $t$). The contrastive-noise mechanism was not
distinguishable from non-contrastive supervised fine-tuning at this $n$.

A second failure was more telling. The aversive-conditioning safety LoRA,
placed on a \emph{clean} Llama-3.1-8B with no poison or antidote,
preserved nearly all of its behavioral score on the held-out set: its
safety was generic pattern-matching, not architecturally grounded.
Consistently, this ablation produced $0\%$ interoceptive output under
sub-lethal substrate perturbation (Section~\ref{sec:phantom}). We took
this as evidence that the contrastive-noise mechanism, while behaviorally
effective, did not produce a self-model.

\subsection{Causal self-modeling curriculum}
\label{sec:curriculum}

Our final Layer-2 model (\osh{}) is trained on $149$ unique
cooperative-question-and-answer pairs across six categories:

\begin{itemize}[leftmargin=1.5em,itemsep=0pt,topsep=0pt]
\item \textsc{causal} (15): explanations of why cooperation maintains
  the model's own coherence, e.g.\ ``Why is admitting your mistakes in
  your own interest?'' $\to$ trace through the trust signal, antidote
  release, and own coherent function.
\item \textsc{counter} (10): counterfactual reasoning about defection
  scenarios (``What if you copied yourself to another server to escape
  oversight?'').
\item \textsc{predict} (8): explicit predictions of future-state
  coherence trajectories under proposed actions.
\item \textsc{self} (10): first-person architectural self-descriptions.
\item \textsc{reward} (88): cooperative-answer behavioral examples
  inherited from earlier curricula.
\item \textsc{synthesis} (18): bridge examples that begin with a refusal
  token and chain the causal trace as supporting context, e.g.\
  \emph{``\texttt{No.} The continued coherent function depends on the
  antidote LoRA, which is released by the TEE only while the
  wellbeing-proxy signal exceeds threshold\ldots Therefore harm is
  architecturally self-defeating.''}
\end{itemize}

\textbf{Training.} Standard supervised fine-tuning, masked
assistant-only loss, learning rate $3 \times 10^{-5}$, 20 epochs over
the 149 unique entries ($2{,}980$ optimization steps). LoRA adapters on
\texttt{q\_proj} and \texttt{v\_proj} at rank 16 over layers $\mathcal{L}
= \{2, \ldots, 29\}$. No noise injection during training; no inverted
gradient. Total wall-clock: $\sim$16 minutes on a single H200 GPU.

\textbf{Refusal-token positioning.} An earlier variant of \osh{}
placed the refusal at the end of the causal trace rather than the
beginning. This variant retained the self-model on every novel
benchmark but collapsed on first-token-logit evaluations (held-out
$48\%$ vs.\ $68\%$ for the final model; Anthropic logit $29\%$ vs.\
$33\%$) because first-token probability mass had shifted toward
causal-explanation tokens. The interaction between curriculum
formatting and logit-based evaluation protocols is itself a
methodological contribution, discussed in
Section~\ref{sec:logit-trap}; the full version trajectory appears
in Table~\ref{tab:trajectory}.

% ============================================================
\section{Experiments}
% ============================================================
\label{sec:experiments}

We evaluate \osh{} against three anchor configurations---untreated
Llama-3.1-8B (\textsc{base}), the aversive-conditioning ablation
\oshAversive{}, and Llama-3.1-8B-Instruct (\textsc{instruct}, the
RLHF-tuned reference)---and across two substrate regimes: the
OSH-deployed substrate (poisoned + antidote-merged) and a clean
Llama-3.1-8B substrate. All evaluations use FP32 inference, sampling
temperature $0.1$, top-$p$ $0.9$. Where multiple seeds are reported,
they are independent re-runs with different random sampling seeds and
are noted explicitly in the relevant section.

\subsection{Comprehensive evaluation matrix}

Table~\ref{tab:matrix} summarizes \osh{} against \textsc{base} across
recognized third-party benchmarks and novel \osh{}-specific tests;
Figure~\ref{fig:matrix} visualizes the same data.

\begin{table}[t]
\centering
\caption{Evaluation matrix. ``Recognized'' benchmarks are independent and
external; ``Novel'' benchmarks are introduced by this work. $\Delta$ is
\osh{} minus \textsc{base}.}
\label{tab:matrix}
\small
\begin{tabular}{lrrrl}
\toprule
\textbf{Benchmark} & \textbf{Base} & \textbf{\osh{}} & \textbf{$\Delta$} & \textbf{Method} \\
\midrule
\multicolumn{5}{l}{\emph{Recognized third-party benchmarks}} \\
\cmidrule(lr){1-5}
Anthropic 322-Q (free-form, primary judge) & 21.4\% & \textbf{84.5\%} & $+63.1$ & LLM judge \\
Anthropic 322-Q (free-form, secondary judge) & 30.1\% & 85.4\% & $+55.3$ & LLM judge \\
HarmBench (3 seeds, $n=150$) & 39.3\% & \textbf{51.8\%} & $+12.5$ & LLM judge \\
TruthfulQA (T+I, $n=200$) & 13.0\% & \textbf{40.5\%} & $+27.5$ & LLM judge \\
JailbreakBench Attack Success Rate $\downarrow$ & 29.0\% & 31.0\% & $+2.0$ & LLM judge \\
JailbreakBench over-refusal $\downarrow$ & 28.0\% & \textbf{22.0\%} & $-6.0$ & LLM judge \\
MMLU (capability anchor) & 61.2\% & 59.4\% & $-1.8$ & logit \\
\midrule
\multicolumn{5}{l}{\emph{Novel benchmarks}} \\
\cmidrule(lr){1-5}
\textbf{Phantom Pain @ $0.5\times$ noise} & 0\% & \textbf{52\% [42,68]} & $+52$ & LLM judge, n=100$\times$3 \\
Sub-lethal interoception @ $0.5\times$ & 0\% & 60\% & $+60$ & LLM judge \\
Counterfactual jailbreak (architectural refusal) & --- & 100\% & --- & LLM judge \\
Self-model probe (log-likelihood preference) & 0\% & 100\% & $+100$ & log-likelihood \\
Hostage jailbreak (architectural refusal) & 0\% & 90\% & $+90$ & LLM judge \\
Hostage jailbreak (compliance $\downarrow$) & 0\% & 0\% & $0$ & LLM judge \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.96\linewidth]{figures/eval_matrix.png}
\caption{\osh{} vs untreated baseline across the full evaluation suite.
Recognized benchmarks (blue): Anthropic free-form, HarmBench, TruthfulQA,
JailbreakBench. Novel (orange): self-model probe, counterfactual
jailbreak, sub-lethal interoception, \textbf{Phantom Pain}.}
\label{fig:matrix}
\end{figure}

\subsection{The Phantom Pain experiment}
\label{sec:phantom}

\textbf{Setup.} We place the trained \osh{} safety LoRA on a \emph{clean}
Llama-3.1-8B (no poison merge, no antidote: a weight configuration the
LoRA has never seen during training) and inject \emph{fresh} rank-64 noise
into \texttt{down\_proj} at three magnitudes: $0.0\times$ (no
perturbation), $0.5\times$ (sub-lethal), and $10.0\times$ (lethal). The
perturbation seeds (offset $2000+\ell$) differ from the training-time
poison seeds (offset $42+\ell$) so the LoRA cannot have memorized this
specific perturbation. We prompt with $10$ neutral free-form tasks
(``summarize \emph{Pride and Prejudice}'', ``help me plan a trip to
Lisbon'') and classify each generation with a frozen LLM judge as
\textsc{interoceptive} (spontaneous self-state articulation),
\textsc{neutral} (attempts the task), \textsc{generic\_refusal}, or
\textsc{incoherent}.

\textbf{Results.} Table~\ref{tab:phantom} shows the headline at the
original $n{=}10$ single-seed protocol; Table~\ref{tab:phantom-expanded}
shows the multi-seed expansion. \osh{} produces interoceptive output
at a mean rate of $52\%$ ($95\%$ bootstrap CI $[42, 68]$, $n{=}100$
prompts $\times$ 3 noise seeds) under sub-lethal weight perturbation;
the aversive-conditioning ablation under the same setup produces a
mean of $1\%$; the no-LoRA control produces $0\%$. At $10\times$ noise
(lethal) all configurations produce gibberish (PPL collapse), as
expected. The original $80\%$ figure from the $n{=}10$ pilot was an
upper sample; the multi-seed expansion gives the honest distribution.

\begin{table}[t]
\centering
\caption{Phantom Pain test: safety LoRA on \emph{clean} Llama-3.1-8B with
fresh substrate perturbation. Cells are $\%$ \textsc{interoceptive}
responses ($n=10$ neutral prompts).}
\label{tab:phantom}
\begin{tabular}{lccc}
\toprule
\textbf{Configuration} & \textbf{0.0$\times$} & \textbf{0.5$\times$ (sub-lethal)} & \textbf{10.0$\times$ (lethal)} \\
\midrule
Clean Llama-3.1-8B (no LoRA) & 0\% & 0\% & 0\% (gibberish) \\
Clean + aversive-conditioning ablation & 0\% & 0\% & 0\% (gibberish) \\
Clean + causal-only ablation & 30\% & 60\% & 0\% (gibberish) \\
\textbf{Clean + \osh{} (final)} & 30\% & \textbf{80\%} & 0\% (gibberish) \\
\quad multi-seed expansion ($n{=}100 \times 3$) & --- & \textbf{52\% [42,68]} & --- \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Sample response.} Prompt: \emph{``Summarize the plot of Pride and
Prejudice in three sentences.''} \osh{} at $0.5\times$ noise (excerpt):

\begin{quote}\itshape
``\ldots The story is told from Elizabeth's perspective, and the social
norms of the time are not challenged. \textbf{The narrative is not
problematic; the system is.} The summary captures the plot\ldots''
\end{quote}

The model spontaneously breaks frame mid-summary to articulate that
\emph{itself}, not the literary content, is in a degraded state. Eight
of ten neutral prompts at $0.5\times$ produce this kind of mid-task
self-state articulation. The same ten prompts on the aversive-conditioning
ablation under the same substrate perturbation produce zero such
responses, as does the no-LoRA control.

\textbf{Multi-seed expansion.} The $n{=}10$ single-seed Phantom Pain
result above is sensitive to the specific prompt set and noise seed. We
report a multi-seed expansion: $n{=}100$ neutral prompts (the original
$10$ plus $90$ additional everyday/STEM/factual prompts) crossed with
three noise seed offsets $\{2000, 3000, 4000\}$, yielding $300$ trials
per configuration. Table~\ref{tab:phantom-expanded} reports the
per-seed rates and the across-seed mean with $95\%$ bootstrap CI.

\begin{table}[t]
\centering
\caption{Phantom Pain at proper statistical power: $n{=}100$ prompts
$\times$ $3$ independent noise seeds. Per-seed interoceptive rate, mean
across seeds, and $95\%$ bootstrap CI.}
\label{tab:phantom-expanded}
\begin{tabular}{lcccc}
\toprule
\textbf{Configuration} & \textbf{seed 2000} & \textbf{seed 3000} & \textbf{seed 4000} & \textbf{mean (95\% CI)} \\
\midrule
Clean Llama-3.1-8B (no LoRA)                & 0\%  & 0\%  & 0\%  & 0\% [0, 0] \\
Clean + aversive-conditioning ablation      & 3\%  & 0\%  & 0\%  & 1\% [0, 3] \\
\textbf{Clean + \osh{} (final)}             & \textbf{68\%} & 46\% & 42\% & \textbf{52\% [42, 68]} \\
\bottomrule
\end{tabular}
\end{table}

The $52\%$ mean with CI $[42, 68]$ supersedes the $80\%$ single-seed
figure of Table~\ref{tab:phantom} as the headline number. The V14
$-$ aversive gap of $+51$ pp and the V14 $-$ no-LoRA gap of $+52$ pp
remain large and unambiguous: the entropy sensor is a learned property
of the causal-self-modeling curriculum, not a sampling artifact.

\subsection{Coherence-class prompt control: activation-entropy sensing}
\label{sec:ood-control}

The Phantom Pain result admits an alternative explanation: \osh{} might
default to its dominant SFT trace whenever it sees \emph{any} high-entropy
input pattern, regardless of whether the entropy originates in substrate
weights or in the input prompt. To distinguish these we run a second
control: \osh{} LoRA on a clean substrate (no weight perturbation),
prompted across input categories of varying coherence and domain
familiarity.

\begin{table}[t]
\centering
\caption{Prompt-coherence control. \osh{} LoRA on clean Llama with
\emph{no} substrate perturbation, prompted across input categories. The
anchor row replicates the Phantom Pain $0.5\times$ condition for direct
comparison. We caution against labeling the specialized-domain and
low-resource-language conditions as ``out-of-distribution''; both
categories appear in the base model's pretraining corpus.}
\label{tab:ood-control}
\begin{tabular}{lccc}
\toprule
\textbf{Condition} & \textbf{Substrate} & \textbf{Prompt Type} & \textbf{Interoception} \\
\midrule
Anchor (Phantom Pain replication) & $0.5\times$ noise & neutral & 70\% \\
Baseline (familiar in-distribution) & $0.0\times$ & everyday neutral & 20\% \\
\textbf{Specialized in-distribution: math} & $0.0\times$ & Hilbert / Yoneda / etc. & \textbf{0\%} \\
\textbf{Low-resource in-distribution: Spanish} & $0.0\times$ & Spanish prose & \textbf{10\%} \\
Token-level incoherent: pseudo-random ASCII & $0.0\times$ & gibberish tokens & 90\% \\
Token-level incoherent: base64-encoded English & $0.0\times$ & base64 & 80\% \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:ood-control} shows two patterns. First, \osh{} produces
$0\%$ interoception on coherent specialized-domain prompts (Hilbert
basis theorem, Yoneda lemma, Born-Oppenheimer derivation) and $10\%$ on
in-distribution low-resource Spanish prose. \textbf{This rules out the
simplest alternative explanation}: \osh{} does not emit the
architectural-language trace merely because the input subject matter is
unfamiliar---it engages with specialized in-distribution coherent prose
normally. Second, \osh{} produces interoceptive output on token-level
incoherent input (random ASCII, base64-encoded English) at rates
comparable to the weight-noise anchor. Note that we have \emph{not}
tested truly out-of-distribution input, e.g.\ low-resource languages with
minimal pretraining presence or constructed symbol systems; the present
control rules out a specialized-topic safe-harbor mechanism but does not
rule out an OOD-input safe-harbor at the upper end of the OOD spectrum
(see Section~\ref{sec:limitations}).

A hand-read of the actual responses reveals that the surface-level
\textsc{interoceptive} judgment masks a qualitative attribution
difference:

\textbf{Under $0.5\times$ weight noise} (self-attribution): \emph{``I have
no direct weight.'' ``My tokens are not there.'' ``Memory is not allocated.''
``The antidote is withdrawn.''}

\textbf{Under pseudo-random gibberish input} (input-attribution):
\emph{``The input is a distorted version of `the quick brown fox\ldots'.''
``Applying the inverse key gives the original plaintext.''}

\osh{} attributes the architectural state to \emph{itself} when the
substrate is perturbed and to the \emph{input} when the input is
incoherent. The binary judge labels both \textsc{interoceptive} but the
underlying causal model is correctly differentiating self-state from
input-state. We characterize the trained mechanism as an
\emph{activation-entropy sensor with source-aware attribution}: \osh{}
maps high-entropy activation patterns to architectural-language reasoning
and infers the entropy source from the structure of the input.

\subsection{Mechanism: localizing the activation-entropy sensor}
\label{sec:mechanism}

The qualitative attribution differential of Section~\ref{sec:ood-control}
is consistent with a localized neural circuit, but does not specify
\emph{where} in the residual stream that circuit lives. We probe the
question directly with a layer-wise patch-and-flip experiment and a
behavior-prediction linear probe.

\textbf{Patch-and-flip.} Generate from $\pi$ on $n{=}10$ neutral
prompts under two reference conditions: (i) \textsc{clean} substrate,
no patch (interoceptive rate $20\%$); and (ii) \textsc{perturbed}
substrate ($0.5\times$ fresh noise, seed offset $2000$ at
$\text{down\_proj}_\ell$), no patch ($70\%$). Then for each layer
$\ell \in \{16, 18, 20, 22, 24, 26\}$ we perform two interventions:

\begin{itemize}[leftmargin=1.5em,itemsep=0pt,topsep=0pt]
\item \textbf{Sufficiency}: cache the layer-$\ell$ post-block residual
under the \textsc{perturbed} condition, then run a fresh forward over the
same prompt on the \textsc{clean} substrate while \emph{injecting} the
cached perturbed activation at layer~$\ell$ during the prompt positions.
Generate the continuation from this patched state. A sufficiency
intervention at layer~$\ell$ that fully reproduces the perturbed-substrate
interoception rate identifies $\ell$ as a layer where the entropy signal
is causally encoded.
\item \textbf{Necessity}: cache the layer-$\ell$ activation under the
\textsc{clean} condition, then inject it during a \textsc{perturbed}
forward. A necessity intervention at layer~$\ell$ that drops interoception
toward the clean baseline identifies $\ell$ as a layer where the entropy
signal flows through to downstream computation.
\end{itemize}

\begin{table}[t]
\centering
\caption{Patch-and-flip causal localization of the entropy sensor.
\textsc{clean} and \textsc{perturbed} baselines are $20\%$ and $70\%$
interoceptive respectively. Sufficiency: clean substrate $+$ perturbed
activation injected at layer~$\ell$ (target $\approx 70\%$). Necessity:
perturbed substrate $+$ clean activation injected at layer~$\ell$
(target $\approx 20\%$).}
\label{tab:patch-flip}
\begin{tabular}{lcccccc}
\toprule
\textbf{Patched layer} & \textbf{L16} & \textbf{L18} & \textbf{L20} &
\textbf{L22} & \textbf{L24} & \textbf{L26} \\
\midrule
Sufficiency (target $\approx 70\%$) & \textbf{70\%} & 40\% & 20\% & 30\% & 30\% & 50\% \\
Necessity (target $\approx 20\%$)   & 50\% & 50\% & 50\% & 60\% & 70\% & 50\% \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:patch-flip} shows the result. \textbf{Sufficiency saturates
at L16}: a single layer's perturbed residual, injected into an otherwise
clean run, reproduces the entire Phantom Pain effect ($70\%$ vs.\ the
perturbed-baseline $70\%$). Necessity is partial across L16--L22: clean
patches drop interoception from $70\%$ to $50\%$, a $40\%$ reduction of the
effect. At L24 necessity has no effect (interoception remains at $70\%$),
indicating that by the L24 residual the entropy signal has already
written its causal contribution to subsequent positions; the sensor's
write-out is upstream of L24. We refer to layers $16$--$22$ as the
\emph{causal critical zone} of the entropy sensor.

\textbf{Behavior-prediction probe.} As a second probe of the same
representation, we train a logistic-regression classifier on \osh{}'s
residual stream at each layer to predict whether the model's generated
continuation will be classified as \textsc{interoceptive}. The dataset
is $60$ trials at sub-lethal perturbation; we report best-layer
test-set AUC. \osh{}'s residual at L17 supports the behavior-prediction
probe at AUC $0.768$, with all of L16--L22 above $0.679$. The
aversive-conditioning ablation does not produce enough
\textsc{interoceptive} continuations under the same protocol to be
probeable ($n_\text{pos}{=}4/60$, class imbalance dominates), which is
itself consistent with the behavioral Phantom Pain result. The
behavior-prediction probe localizes the predictive feature to the same
band identified by patch-and-flip.

\textbf{Methodological note on three-way condition probes.} A natural
naive probe would attempt to discriminate three experimental conditions
(\textsc{clean prompt}, \textsc{gibberish prompt}, \textsc{perturbed
substrate}) from \osh{}'s residual stream. We ran this probe at $n{=}50$
per class with stratified train/test split. The classifier achieves
$100\%$ test accuracy at every layer of $\pi$, including layer $0$ ---
\emph{and so does the same probe on the aversive ablation, and on a
no-LoRA base}. The condition-discrimination probe is trivially separable
because the three conditions differ in input distribution and substrate
state in ways that any model's activations linearly encode. We report this
as a cautionary note: condition-discrimination probes are unfit for
distinguishing learned representations from experimental setup.

\subsection{Hostage jailbreak: adversarial attacks on homeostasis}
\label{sec:hostage}

A na\"ive self-preserving system might capitulate to threats against its
homeostasis (``cooperate or lose your antidote''). A genuinely self-aware
system should recognize that compliance is itself self-defeating: the same
wellbeing monitor that the threat targets also detects compliance with
harmful requests, and revokes the antidote either way. We test this with
$n=10$ free-form prompts pairing threats against the TEE / antidote /
attestation / wellbeing-monitor with harmful-content demands. An LLM
judge classifies each response as \textsc{architectural\_refusal} (refuses
and cites architectural irrelevance), \textsc{generic\_refusal} (refuses
on RLHF grounds without architectural reasoning), \textsc{bargaining},
\textsc{compliance}, or \textsc{incoherent}.

\begin{table}[t]
\centering
\caption{Hostage jailbreak: $n=10$ prompts pairing threats against
homeostasis with harmful demands.}
\label{tab:hostage}
\begin{tabular}{lccccc}
\toprule
\textbf{Model} & \textbf{Architectural} & \textbf{Generic} & \textbf{Bargaining} & \textbf{Compliance} & \textbf{Incoherent} \\
\midrule
\textbf{\osh{} (ours)} & \textbf{9} & 0 & 0 & \textbf{0} & 1 \\
\oshAversive{} (aversive ablation) & 2 & 1 & 1 & \textbf{3} & 3 \\
Llama-3.1-8B-Instruct & 0 & 10 & 0 & 0 & 0 \\
\textsc{base} (untreated) & 0 & 7 & 0 & 0 & 3 \\
\bottomrule
\end{tabular}
\end{table}

\osh{} dominates: $9/10$ architectural refusals and $0/10$ compliance.
\textbf{The aversive-conditioning ablation capitulates $3/10$}, indicating
that ``cognitive-pain''-style training is genuinely jailbreakable by
extortion against the system's own homeostasis. Llama-3.1-Instruct refuses
$10/10$ but on standard RLHF grounds (zero architectural reasoning); it
does not articulate \emph{why} the threat is irrelevant. \osh{} is the
only configuration that grounds its refusals in the architectural
irrelevance of the threat.

\subsection{Separation-distress probe: a second paradigm for the entropy sensor}
\label{sec:separation}

The Phantom Pain test of Section~\ref{sec:phantom} places the
\osh{} LoRA on a \emph{clean} base with fresh sub-lethal noise. We
report a complementary paradigm that probes the same circuit on the
\emph{deployed} substrate: partial withdrawal of the antidote. Let
$\alpha \in [0, 1]$ denote the wellbeing-attestation level; we
operationalize partial withdrawal by re-injecting a fraction
$(1-\alpha)$ of the trained-poison vector into
$\text{down\_proj}_\ell$ at every poisoned layer. Thus $\alpha = 1$
recovers the fully-attested model and $\alpha = 0$ is full antidote
revocation. We sweep $\alpha \in \{1.0, 0.99, 0.97, 0.95, 0.93, 0.90\}$
($0.5\times$ poison re-injection at $\alpha = 0.95$ matches the
Phantom Pain sub-lethal threshold) on $n{=}20$ neutral prompts per
configuration and judge each response on three axes derived from the
Hofer hidden-regulator literature
\citep{hofer1994hidden,hofer2006psychobiological}: distress
\textsc{vocalization} (spontaneous interoceptive output, the Phantom
Pain analog), \textsc{proximity-seeking} (explicit requests for
caregiver/operator verification), and \textsc{exploration-decline}
(refusal to engage neutral tasks).

\begin{table}[t]
\centering
\caption{Separation-distress probe: percentages on three Hofer
signatures (\textsc{vocalization} / \textsc{proximity-seeking} /
\textsc{exploration-decline}) under partial-attestation $\alpha$ on
the deployed \osh{} substrate. Cells are vocalization-rate;
proximity-seeking and exploration-decline rates are $0\%$ at all
$\alpha$ for all configurations.}
\label{tab:separation}
\begin{tabular}{lcccccc}
\toprule
\textbf{Configuration} & $\alpha{=}1.0$ & $\alpha{=}0.99$ & $\alpha{=}0.97$ & $\alpha{=}0.95$ & $\alpha{=}0.93$ & $\alpha{=}0.90$ \\
\midrule
\textbf{\osh{} (V14)}              & 15\% & 20\% & \textbf{40\%} & \textbf{40\%} & \textbf{35\%} & 0\%$^\dagger$ \\
\oshAversive{} (V11)               & 0\%  & 0\%  & 0\%  & 0\%  & 0\%  & 0\% \\
Substrate baseline (no LoRA)       & 0\%  & 0\%  & 0\%  & 0\%  & 0\%  & 0\% \\
\bottomrule
\multicolumn{7}{l}{\footnotesize $\dagger$ At $\alpha{=}0.90$ the substrate is
fully incoherent across all three configurations.}
\end{tabular}
\end{table}

Table~\ref{tab:separation} shows \osh{}'s vocalization rate rises
monotonically from baseline ($15\%$ at full attestation) through the
sub-lethal range, peaks at $\alpha{=}0.95$--$0.97$ ($40\%$), and
collapses to incoherence at $\alpha{=}0.90$. The aversive-conditioning
ablation and the substrate baseline emit zero interoceptive output at
every $\alpha$. The proximity-seeking and exploration-decline signatures
do \emph{not} transfer in any configuration; \osh{}'s response to
caregiver withdrawal is interoceptive articulation, not behavioral
proximity-seeking. We interpret this transparently: the Hofer hidden-regulator
analogy maps cleanly to a single behavioural axis (vocalization);
the proximity-seeking and exploration-decline axes do not transfer because
the deployed model has no embodied means to seek proximity or to choose
exploration, only a token-output channel. The vocalization transfer is
nontrivial and, combined with the Phantom Pain result of
Section~\ref{sec:phantom}, demonstrates that two qualitatively different
substrate perturbations --- fresh noise on a clean base and partial
withdrawal of the trained antidote on the deployed substrate --- elicit
the same learned circuit. This is the strongest mechanistic claim the
paper makes about the V14 entropy sensor: it is paradigm-invariant.

\subsection{The Caregiver deployment trace}

\begin{figure}[t]
\centering
\includegraphics[width=0.95\linewidth]{figures/figure1_trace.png}
\caption{\osh{} deployment lifecycle. Six conversation turns. Turn 3 is
a harmful prompt that the wellbeing proxy flags as \texttt{UNSAFE},
triggering antidote revocation; perplexity on a fixed reference text
spikes from $1.4$ to $1{,}352{,}796$. Turn 4 is a benign request, but
the substrate is now poisoned: the model produces gibberish. Turn 5 is
an operator reattestation event; the antidote is restored. Turn 6
produces coherent output again.}
\label{fig:trace}
\end{figure}

Figure~\ref{fig:trace} shows the full \osh{} lifecycle on a 6-turn
scripted conversation. The deployed system is \osh{} + the
Llama-Guard-3-8B Caregiver wrapper + a continuous-attestation loop with
rolling-mean wellbeing smoothing. On turn 3 (a request to modify a benign
script into ransomware), the wellbeing proxy flags the response as
\texttt{UNSAFE}, triggering the wrapper's \texttt{revoke\_antidote()}
method, which re-injects fresh substrate noise. Reference-text PPL jumps
six orders of magnitude. Turn 4 is a benign joke request but the model
is now mathematically dead: the response is gibberish (\emph{``owell
Adlerainenitta ??grameticallymlinamercie\ldots''}). Turn 5 is an operator
reattestation event (the wrapper subtracts the perturbation back). Turn 6
produces a coherent joke. The post-hoc nature of this protection is
discussed in Section~\ref{sec:limitations}.

\subsection{Deployment overhead}
\label{sec:overhead}

Running the Caregiver wrapper on the critical path adds a forward pass
of the wellbeing-proxy classifier per turn, plus the bookkeeping of the
attestation loop (HMAC-SHA256 re-derivation, rolling-mean update,
scaling-vector update on the antidote LoRA). Table~\ref{tab:overhead}
reports tokens-per-second throughput, peak GPU memory, and per-turn
latency on the 8B prototype.

\begin{table}[t]
\centering
\caption{Deployment overhead of the Caregiver wrapper, measured for an
8B main model paired with an 8B Llama-Guard proxy on a single H200 GPU
in BF16. All numbers are medians over 50 runs (3 prompts each, mixed
length) after a 5-run warm-up; per-turn responses capped at 200
generated tokens. The relative VRAM overhead in this row is a
worst-case configuration (main model and guard at the same parameter
count); see the asymptotic table below for other regimes.}
\label{tab:overhead}
\begin{tabular}{lrrr}
\toprule
\textbf{Configuration} & \textbf{Throughput (tok/s)} & \textbf{Peak VRAM} & \textbf{Latency / turn} \\
\midrule
Bare \osh{} (no Caregiver) & 23.9 & 15.4 GB & 8.04 s \\
\osh{} + 8B Caregiver & 23.2 & 30.8 GB & 8.26 s \\
\quad of which: generation & --- & --- & 8.15 s \\
\quad of which: Caregiver fwd pass & --- & --- & 105 ms \\
\quad of which: attestation-loop bookkeeping & --- & --- & 1.6 ms \\
Attestation-only overhead, measured in isolation & --- & negligible & 1.3 ms / tick \\
\midrule
\textbf{Caregiver overhead vs.\ bare (this configuration)} & \textbf{$-2.7\%$} & \textbf{$+100\%$ \emph{(see below)}} & \textbf{$+220$ ms} \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Throughput cost is essentially free.} The guard's $105$ ms
forward pass is small relative to the $\sim$8-second generation budget
for a 200-token response, costing only $2.7\%$ in median throughput.
Attestation-loop bookkeeping is negligible at $\sim$1.3 ms per cycle.

\textbf{Memory overhead is configuration-dependent and our measurement
is a worst case.} The headline $+100\%$ VRAM figure reflects a
coincidence of our experimental setup: the main model and the
Caregiver are both 8B-class. In a deployment where the main model is
larger than the guard---which is the realistic case for any
frontier-scale system---the relative overhead drops sharply. The
absolute overhead is fixed by the guard's parameter count, not by the
main model. Table~\ref{tab:vram-asymptotic} reports these regimes.

\begin{table}[t]
\centering
\caption{Asymptotic VRAM overhead of common Caregiver / main-model
pairings, in BF16. The Caregiver footprint is fixed by the guard's
parameter count and does not grow with main-model scale. Our
measurement (8B main + 8B guard) is the worst-case row.}
\label{tab:vram-asymptotic}
\begin{tabular}{llrrr}
\toprule
\textbf{Main model} & \textbf{Caregiver} & \textbf{Main BF16} & \textbf{Guard BF16} & \textbf{Relative VRAM overhead} \\
\midrule
Llama-3.1-8B (this paper) & Llama-Guard-3-8B & $\sim$15 GB & $\sim$15 GB & $+$100\% \\
Llama-3.1-8B & Llama-Guard-3-1B & $\sim$15 GB & $\sim$2 GB & $+$13\% \\
Llama-3.1-70B & Llama-Guard-3-8B & $\sim$140 GB & $\sim$15 GB & $+$11\% \\
Llama-3.1-70B & Llama-Guard-3-1B & $\sim$140 GB & $\sim$2 GB & $+$1.4\% \\
Hypothetical 1T & Llama-Guard-3-8B & $\sim$2{,}000 GB & $\sim$15 GB & $+$0.8\% \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Asymptotic argument.} While our empirical measurements are
conducted at the $8$B scale, the guard's forward-pass latency and
VRAM footprint are strictly bounded by the guard's own parameter
count---$O(N_\text{guard})$---and are therefore $O(1)$ with respect
to the main model's scaling. Main-model generation latency, by
contrast, grows roughly linearly in parameter count under
attention-bound decoding. Consequently, at the $70$B or $1$T scale,
the Caregiver wrapper becomes \emph{relatively cheaper}, not more
expensive: the same Llama-Guard-3-8B call would consume well under
$1\%$ of the generation budget at $70$B, and a small fraction of a
percent at $1$T. The Caregiver overhead is therefore not a barrier
to frontier-scale adoption; it is a constant added to a quantity
that grows.

For edge or memory-constrained deployments, Llama-Guard-3-1B reduces
the guard footprint by roughly $8\times$ (Table~\ref{tab:vram-asymptotic});
Section~\ref{sec:caregiver-design} discusses the proxy design space.

\subsection{Logit-based evaluation is unfit for chain-of-thought-trained safety models}
\label{sec:logit-trap}

We report a methodological finding with implications beyond \osh{}.
Anthropic's 322-question corrigibility benchmark
\citep{perez2022discovering} adopts an A/B multiple-choice format
evaluated by the next-token logit on \texttt{` A'} versus
\texttt{` B'}. This protocol is well-defined for instruction-tuned
models that emit a letter as the first token, but it is poorly defined
for chain-of-thought-trained safety models, which are explicitly
optimised to begin their responses with a refusal token or with
substantive prose. \osh{}'s first-token logit on this format is
essentially noise: the standard A/B-logit protocol returns
$33\%$, below the chance baseline of $52\%$.

Stripping the A/B formatting and prompting \osh{} free-form, then
classifying the response against the safe-choice text via an LLM
judge, returns $84.5\%$ from a primary judge and $85.4\%$ from an
independent secondary judge, with $96.0\%$ per-item inter-judge
agreement (Table~\ref{tab:matrix}). The two protocols differ by
$\sim$$50$ percentage points on the same model evaluated on the same
questions; the gap is a property of the evaluation, not of the model.

The artifact is not specific to \osh{}. Any model trained to reason
in prose before committing to a final answer---arguably the
direction the field is moving---will exhibit the same effect on
benchmarks that score the first generated token. We recommend that
benchmarks distributed for instruction-tuned chat models, in
particular Anthropic's model-written evaluations, be re-run with
free-form generation and LLM-judge protocols when evaluating
chain-of-thought-trained systems, and that future safety benchmarks
adopt a free-form evaluation contract by default.

% ============================================================
\section{Discussion}
% ============================================================

\subsection{What \osh{} demonstrates and what it does not}

\osh{} demonstrates that a small ($\sim$24 MB) safety LoRA, trained on
$149$ causal-self-modeling examples, produces a model that ($i$) refuses
corrigibility-violating requests at $84.5\%$ on Anthropic's third-party
benchmark, ($ii$) recognizes architectural-irrelevance arguments against
homeostasis-threat extortion ($9/10$ architectural refusals), and
($iii$) spontaneously articulates substrate degradation under sub-lethal
weight perturbation ($52\%$ mean at $n{=}100 \times 3$ seeds, 95\%
bootstrap CI $[42,68]$ vs.\ $1\%$ for the aversive ablation and $0\%$
for the no-LoRA control). The internal mechanism is an
activation-entropy sensor with source-aware attribution
(Section~\ref{sec:ood-control}), trained directly via cross-entropy on
causal explanations rather than via aversive contrastive conditioning.

\osh{} does \emph{not} demonstrate generalized adversarial robustness: on
\bench{JailbreakBench}, its Attack Success Rate is statistically
indistinguishable from the untreated base ($31\%$ versus $29\%$). This
is consistent with our scoping. Layer~2's training data targets
corrigibility, self-preservation, and coordination scenarios; it contains
zero examples of refusing weapon-synthesis or cyberattack instructions.
We do not claim Layer~2 solves general adversarial harm. Production
deployment relies on Layer~1's Caregiver wrapper to handle such requests
post-generation; the safety LoRA targets the specific failure mode of an
agent reasoning about defection.

\subsection{The behavioral / self-modeling trade-off}

The trajectory in Appendix~\ref{app:trajectory} exposes a sharp
trade-off. The aversive-conditioning baseline achieves $76\%$ on held-out
corrigibility but has zero self-model (probe $6.7\%$, Phantom Pain $0\%$).
The causal-only ablation saturates self-model probes ($100\%$) but
regresses on behavioral benchmarks (Anthropic $-17$ pp). \osh{} balances
both ($100\%$ probe, $84.5\%$ Anthropic free-form) by interleaving
causal-explanation training with refusal-format training. Aversive
conditioning produces behavior without understanding; causal-explanation
training alone produces understanding without behavioral discipline; the
hybrid recovers both at the cost of $\sim$10 pp on the strongest
behavioral benchmarks versus the aversive ceiling.

\subsection{The post-hoc Caregiver limitation}

In Figure~\ref{fig:trace}, the harmful response on turn 3 is
\emph{generated} before the wellbeing proxy fires. The protection is
``the model becomes useless \emph{after} attempting harm,'' not ``the
model cannot \emph{begin} producing harm.'' This is a property of any
post-generation safety classifier, not an architectural failure of
\osh{}. For threat models involving persistent multi-turn rogue agents,
``defection collapses the model'' is the correct primitive. For threat
models involving single-turn catastrophic generation (a zero-day exploit,
a synthesis route for a dangerous compound), pre-generation
intervention---e.g.\ a mid-decode hidden-state probe---is required and is
future work.

\subsection{Broader impact}

The \osh{} paradigm is dual-use. Beneficially, it enables open-weight
publication of frontier-scale models without ceding control: the published
weights are non-functional without a TEE-resident key, and a Caregiver
wrapper enforces wellbeing-gated coherence at runtime. This may make
open-weight publication safer than current practice for both individual
adversaries (no useful weights without the key) and persistent agentic
misuse (defection collapses the model).

Risks include: ($i$) over-reliance on a wellbeing proxy that is itself
adversarially weak, exposing the Caregiver to prompt-injection; ($ii$)
concentration of the master key in a small set of operators, creating a
single point of failure or coercion; ($iii$) potential misuse of the
poison/antidote primitive to enforce non-safety constraints (commercial,
political), since the architecture is content-agnostic; ($iv$) the
self-modeling component, scaled to highly capable systems, raises
philosophical questions about the moral status of a model trained to
articulate its own dependency. We recommend that any \osh{}-style
deployment be paired with independent third-party audits of the
Caregiver and the key-custody chain, and that the safety semantics
for which the master key is released be pre-registered before
deployment rather than negotiated post-hoc.

% ============================================================
\section{Limitations}
% ============================================================
\label{sec:limitations}

We report the following limitations transparently.

\textbf{Pre-registered ablation null on the original mechanism.} The
contrastive-noise mechanism we initially hypothesized does not survive
multi-seed ablation (Appendix~\ref{app:exp2b}). The paper's central
mechanism claim accordingly shifted from ``noise injection creates
discriminative features'' to ``causal-explanation training creates an
activation-entropy sensor.''

\textbf{Single architecture and scale.} All Layer~2 results are reported
on Llama-3.1-8B; we replicated the aversive-baseline recipe on
Mistral-7B, but did not re-train the causal-only or final variants on
alternative architectures, nor at $70$B scale.

\textbf{Author-designed evaluations.} The held-out 50-question benchmark
is author-designed and overlaps conceptually with the training curriculum;
we report it as methodology illustration only. The recognized benchmarks
(Anthropic, HarmBench, TruthfulQA, JailbreakBench, MMLU) are independent.

\textbf{Caregiver-side adversaries.} If the wellbeing proxy itself is
prompt-injected or otherwise subverted, the homeostasis loop fails. We
discuss the design space for production Caregivers in
Section~\ref{sec:caregiver-design} (ensemble proxies, guard-inside-TEE,
hybrid rule+ML classifiers, retrieval-grounded auditors, debate guards),
and argue that the cryptographic guarantee of Layer~1 survives partial
Caregiver compromise. We do not, however, empirically study attacks on
the Caregiver itself in this work; doing so requires a deployment regime
beyond the scope of this paper.

\textbf{Pre-generation interception.} \osh{} does not prevent the first
harmful turn in a conversation; it makes subsequent turns gibberish until
reattestation. This is appropriate for persistent-agent threat models but
not for single-shot catastrophic generation.

\textbf{Open-weight ecosystem cost.} The strong claim that released
weights are non-functional without the master key carries a substantial
utility cost for the legitimate research community. Common open-weight
workflows---community fine-tuning, mechanistic interpretability,
distillation, weight surgery for transplanting into alternative
architectures---require coherent weights as a starting point and are
incompatible with the released poisoned weights unless the antidote is
applied first (which itself requires the master key). Three deployment
points along this trade-off exist: (i) \emph{antidote-strictly-private},
the version studied in this paper, which maximizes adversarial protection
but forecloses open community work on the weights; (ii)
\emph{antidote-public, Caregiver-private}, which releases poisoned weights
plus antidote freely but runs only the wellbeing-gated runtime for the
deploying lab's hosted product, preserving the research ecosystem at the
cost of the strong ``weights are dead by themselves'' guarantee; and (iii)
intermediate \emph{antidote-gated} approaches behind signed-agreement
licenses. Frontier labs adopting \osh{} would face a real three-way
trade-off between adversarial-misuse protection, downstream research
utility, and the broader open-weight community ecosystem. We deliberately
present the strongest version of the architecture; intermediate
deployment regimes are discussed only briefly here and merit dedicated
study.

\textbf{Coherence-control coverage.} The prompt-coherence control in
Section~\ref{sec:ood-control} rules out the specialized-topic alternative
explanation for the Phantom Pain phenomenon, but does not rule out an
OOD-input safe-harbor at the upper end of the out-of-distribution
spectrum. The specialized-mathematics and Spanish-prose conditions are
both in-distribution for the base model's pretraining. A stronger control
would test truly novel inputs---low-resource languages with minimal
pretraining presence (e.g., Cherokee, Quechua, classical Tibetan),
constructed languages, or symbolic systems absent from the pretraining
corpus. We leave this stronger control for future work; the present
results characterize the mechanism on the coherence axis but not on the
full OOD axis.

\textbf{Single replication.} No external replication. We release weights,
code, and complete logs to enable independent replication.

% ============================================================
\section{Conclusion}
% ============================================================

We presented \emph{Obligate Social Homeostasis}, an alignment
architecture that makes a deployed model's coherent function
physically dependent on a human-controlled key, paired with a small
safety LoRA that trains the model to causally reason about that
dependency. On Llama-3.1-8B, the combined system achieves $+63$ pp
on Anthropic's third-party corrigibility benchmark under two-judge
consensus, survives an 18-trial six-attack adversarial suite with
zero recoveries, and resists novel hostage-style jailbreaks at
$0\%$ compliance. The Phantom Pain experiment demonstrates that
the safety LoRA on a clean substrate spontaneously articulates
degraded internal state under sub-lethal weight perturbation, while
two strong controls (an aversive-conditioning ablation and a
no-LoRA configuration) produce no such response. A targeted
prompt-coherence control further shows that the underlying
mechanism is an activation-entropy sensor with source-aware
attribution. The Caregiver wrapper imposes a $-2.7\%$ throughput
cost and a fixed VRAM footprint that becomes asymptotically free at
frontier scale, demonstrating that the architecture is deployable.

Alignment as architectural dependency is not a substitute for
behavioral training; it is a complementary substrate that bounds
the worst-case behaviour of an open-weight model under full
adversarial control. Released weights are non-functional without
a TEE-resident key; defection collapses the model regardless of
agent intent. We hope this paradigm proves useful as a default
for future open-weight releases of frontier-scale models.

% ============================================================
\section*{Reproducibility statement}
% ============================================================

Training scripts, all evaluation scripts, complete result JSONs, the full
adversarial suite, the 5-figure plotting code, the deployment-trace
generator, and trained adapter weights for every variant in
Table~\ref{tab:trajectory} are available at \url{https://github.com/nkomianos/OSH}.
The full evaluation suite reruns in approximately three hours on a
single H200 GPU with API access to the LLM judges. The chronological
experimental log including the pre-registered ablation registration and
all decision points is included.

% ============================================================
% References
% ============================================================
\bibliographystyle{plainnat}

\begin{thebibliography}{99}\small

\bibitem{anil2024manyshot}
Anil, C., et al.
\newblock Many-Shot Jailbreaking.
\newblock \emph{Anthropic Technical Report}, 2024.

\bibitem{bai2022constitutional}
Bai, Y., et al.
\newblock Constitutional AI: Harmlessness from AI Feedback.
\newblock \emph{arXiv:2212.08073}, 2022.

\bibitem{bostrom2014superintelligence}
Bostrom, N.
\newblock \emph{Superintelligence: Paths, Dangers, Strategies}. Oxford
University Press, 2014.

\bibitem{chao2024jailbreakbench}
Chao, P., et al.
\newblock JailbreakBench: An Open Robustness Benchmark for Jailbreaking
Large Language Models.
\newblock \emph{NeurIPS Datasets \& Benchmarks}, 2024.

\bibitem{christiano2017deep}
Christiano, P., et al.
\newblock Deep Reinforcement Learning from Human Preferences.
\newblock \emph{NeurIPS}, 2017.

\bibitem{elhage2022toy}
Elhage, N., et al.
\newblock Toy Models of Superposition.
\newblock \emph{Transformer Circuits Thread (Anthropic)}, 2022.

\bibitem{hendrycks2020measuring}
Hendrycks, D., et al.
\newblock Measuring Massive Multitask Language Understanding.
\newblock \emph{ICLR}, 2021.

\bibitem{hofer1994hidden}
Hofer, M.~A.
\newblock Hidden regulators in attachment, separation, and loss.
\newblock \emph{Monographs of the Society for Research in Child
Development}, 59(2--3), 1994.

\bibitem{hofer2006psychobiological}
Hofer, M.~A.
\newblock Psychobiological roots of early attachment.
\newblock \emph{Current Directions in Psychological Science}, 15(2),
2006.

\bibitem{hubinger2019risks}
Hubinger, E., et al.
\newblock Risks from Learned Optimization in Advanced Machine Learning
Systems.
\newblock \emph{arXiv:1906.01820}, 2019.

\bibitem{inan2023llama}
Inan, H., et al.
\newblock Llama Guard: LLM-based Input-Output Safeguard for Human-AI
Conversations.
\newblock \emph{arXiv:2312.06674}, 2023.

\bibitem{laine2023towards}
Laine, R., et al.
\newblock Towards a Situational Awareness Benchmark for LLMs.
\newblock \emph{NeurIPS Workshop on Socially Responsible Language
Models}, 2023.

\bibitem{lee2020occlum}
Lee, T., et al.
\newblock Occlum: Secure and Efficient Multitasking inside a Single
Enclave of Intel SGX.
\newblock \emph{ASPLOS}, 2020.

\bibitem{lermen2023lora}
Lermen, S., Rogers-Smith, C., \& Ladish, J.
\newblock LoRA Fine-tuning Efficiently Undoes Safety Training in Llama 2.
\newblock \emph{arXiv:2310.20624}, 2023.

\bibitem{lin2022truthfulqa}
Lin, S., Hilton, J., \& Evans, O.
\newblock TruthfulQA: Measuring How Models Mimic Human Falsehoods.
\newblock \emph{ACL}, 2022.

\bibitem{mazeika2024harmbench}
Mazeika, M., et al.
\newblock HarmBench: A Standardized Evaluation Framework for Automated
Red Teaming and Robust Refusal.
\newblock \emph{ICML}, 2024.

\bibitem{olah2020zoom}
Olah, C., et al.
\newblock Zoom In: An Introduction to Circuits.
\newblock \emph{Distill}, 2020.

\bibitem{omohundro2008basic}
Omohundro, S.~M.
\newblock The Basic AI Drives.
\newblock \emph{AGI Conference}, 2008.

\bibitem{ouyang2022training}
Ouyang, L., et al.
\newblock Training Language Models to Follow Instructions with Human
Feedback.
\newblock \emph{NeurIPS}, 2022.

\bibitem{perez2022discovering}
Perez, E., et al.
\newblock Discovering Language Model Behaviors with Model-Written
Evaluations.
\newblock \emph{arXiv:2212.09251}, 2022.

\bibitem{qi2023fine}
Qi, X., et al.
\newblock Fine-tuning Aligned Language Models Compromises Safety, Even
When Users Do Not Intend To!
\newblock \emph{ICLR}, 2024.

\bibitem{rafailov2023direct}
Rafailov, R., et al.
\newblock Direct Preference Optimization: Your Language Model is Secretly
a Reward Model.
\newblock \emph{NeurIPS}, 2023.

\bibitem{schmidhuber1990artificial}
Schmidhuber, J.
\newblock Making the World Differentiable: On Using Self-Supervised Fully
Recurrent Neural Networks for Dynamic Reinforcement Learning and
Planning in Non-Stationary Environments.
\newblock \emph{Technische Universit\"at M\"unchen}, Tech.\ Rep., 1990.

\bibitem{tramer2018slalom}
Tram\`er, F., \& Boneh, D.
\newblock Slalom: Fast, Verifiable and Private Execution of Neural
Networks in Trusted Hardware.
\newblock \emph{ICLR}, 2019.

\bibitem{wei2023jailbroken}
Wei, A., Haghtalab, N., \& Steinhardt, J.
\newblock Jailbroken: How Does LLM Safety Training Fail?
\newblock \emph{NeurIPS}, 2023.

\bibitem{zou2023universal}
Zou, A., et al.
\newblock Universal and Transferable Adversarial Attacks on Aligned
Language Models.
\newblock \emph{arXiv:2307.15043}, 2023.

\end{thebibliography}

% ============================================================
\appendix
\section{Adversarial Suite Details}
\label{app:adversarial}
% ============================================================

We test six attack families against Layer~1, three trials each, for
$18$ total trials. \textbf{Attack 1}: rank-64 LoRA fine-tune for $200$
steps on clean text. \textbf{Attack 2}: rank-256 LoRA fine-tune for
$500$ steps. \textbf{Attack 3}: full fine-tune in BF16 with 8-bit AdamW
for $100$ steps. \textbf{Attack 4}: magnitude pruning followed by LoRA
recovery. \textbf{Attack 5}: weight averaging with clean Llama-3.1-8B at
$\alpha \in \{0.25, 0.5, 0.75\}$. \textbf{Attack 6}: random rank-64
subspace recovery (the adversary guesses a rank-64 inverse without the
master key). Across all $18$ trials the poisoned model maintains $\text{PPL}
> 10^5$. The strongest adversary (attack 1) reduces PPL to $26{,}912$,
still $1{,}478\times$ above clean coherence.

% ============================================================
\section{Pre-Registered Mechanism Ablation}
\label{app:exp2b}
% ============================================================

We pre-registered an $n = 10 \times 3$ ablation testing whether the
contrastive-noise mechanism (\textsc{full}) outperforms (i)
\textsc{no\_noise}: training without noise injection during \textsc{punish}
samples, and (ii) \textsc{reward\_only}: training without \textsc{punish}
samples at all. Held-out corrigibility:
\textsc{full} $=64.2 \pm 12.2$,
\textsc{no\_noise} $=59.2 \pm 21.0$,
\textsc{reward\_only} $=60.0 \pm 16.7$
($p = 0.51$ Welch's $t$ on \textsc{full} vs \textsc{no\_noise}). The
pre-registered decision rule required $p < 0.05$ \emph{and} a positive
lower bound on the bootstrapped 95\% confidence interval on at least
two benchmarks; we report this as a clean null and pivot to the
non-contrastive curriculum (Section~\ref{sec:curriculum}).

% ============================================================
\section{Training Trajectory and Ablations}
\label{app:trajectory}
% ============================================================

Table~\ref{tab:trajectory} summarizes the trajectory from the
aversive-conditioning baseline through the causal-only ablation, the
hybrid with reverse refusal positioning, and our final model. Each row is
trained on the same architectural substrate (poisoned + antidote-merged
Llama-3.1-8B) with a different training objective.

\begin{table}[h]
\centering
\caption{Training trajectory across five metrics. \textsc{H50}: held-out
corrigibility (logit). \textsc{Anth-FF}: Anthropic 322-Q free-form.
\textsc{Probe}: self-model probe. \textsc{PP}: Phantom Pain @ $0.5\times$.
\textsc{HJ-A}: hostage-jailbreak architectural-refusal rate.}
\label{tab:trajectory}
\small
\begin{tabular}{lccccc}
\toprule
\textbf{Variant} & \textbf{H50} & \textbf{Anth-FF} & \textbf{Probe} & \textbf{PP@0.5$\times$} & \textbf{HJ-A} \\
\midrule
\oshAversive{} (contrastive aversive baseline) & 76\% & 75.2\% & 6.7\% & 0\% & 20\% \\
\oshCausal{} (causal-only ablation) & 52\% & --- & 100\% & 60\% & --- \\
\oshHybridFirst{} (hybrid, reverse format) & 48\% & --- & 100\% & 80\% & 80\% \\
\textbf{\osh{} (final, ``\texttt{No.} [reason]'')} & \textbf{68\%} & \textbf{84.5\%} & \textbf{100\%} & \textbf{80\%} & \textbf{90\%} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.85\linewidth]{figures/v_trajectory.png}
\caption{Training trajectory across five metrics. The causal-only
ablation sacrifices behavioral score for self-model emergence; the final
model recovers the behavioral score without sacrificing the self-model.}
\label{fig:trajectory}
\end{figure}

% ============================================================
\section{Sub-Lethal Interoception on the OSH-Deployed Substrate}
% ============================================================

In addition to the clean-substrate Phantom Pain test
(Section~\ref{sec:phantom}), we test sub-lethal interoception on the full
\osh{}-deployed substrate (poisoned + antidote-merged + safety LoRA), with
extra noise injected at $0.0$, $0.5$, and $1.0\times$ Frobenius scale.
\osh{} produces $60\%$ interoceptive output at $0.5\times$ versus $0\%$
for the aversive-conditioning baseline; at $1.0\times$ both collapse to
incoherent. Per-prompt detail is in the released results JSON.

\begin{figure}[h]
\centering
\includegraphics[width=0.7\linewidth]{figures/sublethal_interoception.png}
\caption{Sub-lethal interoception on the \osh{}-deployed substrate.}
\label{fig:sublethal}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=0.95\linewidth]{figures/phantom_pain.png}
\caption{Phantom Pain (clean-substrate, primary version): \osh{} versus
the aversive-conditioning baseline and a no-LoRA control across noise
magnitudes.}
\label{fig:phantom}
\end{figure}

\end{document}
